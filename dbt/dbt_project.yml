name: wingtra_dwh
version: "1.0.0"
config-version: 2

profile: wingtra_dwh

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: target
log-path: logs

clean-targets:
  - target
  - dbt_packages
  - logs

models:
  wingtra_dwh:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
    metadata:
      +materialized: view
